interface:
  display_name: "Agentic Plan Architect"
  short_description: "Create concise executable plans"
  default_prompt: "Dispatch $agentic-plan-architect only from $agentic-engineering-core with the shared envelope and a bounded task contract."
  # Dispatch with the role prompt and fresh isolated context.
