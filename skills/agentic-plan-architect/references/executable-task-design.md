# Executable task design

A task should represent one outcome, usually one concern, with explicit files, acceptance, and verification. Split only when dependencies, ownership, or independent delivery require it. Avoid microtasks that merely restate editing steps.
