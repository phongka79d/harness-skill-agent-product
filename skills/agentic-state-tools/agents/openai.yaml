interface:
  display_name: "Agentic State Tools"
  short_description: "Generate and validate runtime state"
  default_prompt: "Use $agentic-state-tools to generate, validate, and rebuild project runtime state."
