# Exploration protocol

Prefer entry points, direct callers, direct dependencies, and nearby repository instructions. Report evidence with exact file paths and symbols. Separate observed behavior from inference.
