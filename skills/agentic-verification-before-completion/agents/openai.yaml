interface:
  display_name: "Agentic Verification Before Completion"
  short_description: "Verify claims with current evidence"
  default_prompt: "Dispatch $agentic-verification-before-completion only from $agentic-engineering-core with the shared envelope and a bounded task contract."
  # Dispatch with the role prompt and fresh isolated context.
