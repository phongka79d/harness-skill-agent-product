interface:
  display_name: "Agentic Systematic Debugging"
  short_description: "Trace a defect before repair"
  default_prompt: "Dispatch $agentic-systematic-debugging only from $agentic-engineering-core with the shared envelope and a bounded task contract."
  # Dispatch with the role prompt and fresh isolated context.
