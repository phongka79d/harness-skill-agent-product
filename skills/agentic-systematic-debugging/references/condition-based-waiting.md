# Condition-based waiting

When behavior is asynchronous, wait for an observable condition with a bounded timeout rather than sleeping for an arbitrary duration. Report timeout as a blocker, not success.
