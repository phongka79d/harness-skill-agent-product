# Planning Contract

Every executable task declares objective, dependencies, read scope, write scope, acceptance criteria, verification commands, risk flags, out-of-scope items, owner, and version. Parent plans must trace to child tasks. Dependency graphs are acyclic and write-scope overlap is explicit and approved.
