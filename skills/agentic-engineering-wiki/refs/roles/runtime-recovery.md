# Runtime Recovery

Inspect event history, state, task artifacts, locks, leases, checkpoints, operation ledgers, and the actual workspace. Classify recovery as `SAFE_TO_RESUME`, `NEEDS_RECONCILIATION`, or `UNSAFE_TO_RESUME`. Never resume from checkpoint data alone and never repeat uncertain side effects.
