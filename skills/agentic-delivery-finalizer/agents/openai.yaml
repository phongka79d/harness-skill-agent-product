interface:
  display_name: "Agentic Delivery Finalizer"
  short_description: "Control high-impact delivery actions"
  default_prompt: "Use $agentic-delivery-finalizer only when merge, push, deployment, release, or destructive cleanup needs an explicit decision."
